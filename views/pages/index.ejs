<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <title>Helpdesk</title>
    <style>
      body {
        min-height: 100vh;
      }
    </style>
  </head>
  <body class="flex justify-items-center bg-green-400">
    <main class="container mx-auto p-8">
      <div
        class="p-6 max-auto bg-white rounded-xl shadow-lg flex items-center space-x-4"
      >
        <div class="w-full">
          <p class="px-8 text-2xl">Helpdesk Request Form</p>
          <% if (payload.length) { %>
          <div
            class="border flex text-gray-700 items-center px-4 rounded h-12 w-14 <%=payload[0] === 'ok' ? 'border-green-600' : 'border-red-600' %> mx-8"
          >
            สถานะ:&nbsp;<p class="bold <%=payload[0] === 'ok' ? 'text-green-600' : 'text-red-600' %> "><%= payload[0] === 'ok' ? 'ส่งข้อมูลสำเร็จ' : 'ส่งข้อมูลไม่สำเร็จ' %></p>
          </div>
          <%} %>

          <form class="px-8 pt-6 pb-8 mb-4" method="POST" action="/action">
            <label
              class="block text-gray-700 text-sm font-bold mb-2"
              for="fullname"
            >
              ชื่อ-นามสกุล
            </label>
            <input
              class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline mb-4"
              id="fullname"
              name="fullname"
              type="text"
              placeholder="Name Surname"
              required
            />
            <label
              class="block text-gray-700 text-sm font-bold mb-2"
              for="email"
            >
              อีเมล
            </label>
            <input
              class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline mb-4"
              id="email"
              type="email"
              name="email"
              placeholder="helpdesk@info.com"
              requireds
            />
            <div class="flex">
              <div class="mb-4 pr-2 grow w-full">
                <label
                  class="block text-gray-700 text-sm font-bold mb-2"
                  for="department"
                >
                  แผนก
                </label>
                <input
                  class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
                  id="department"
                  name="department"
                  type="text"
                  placeholder="Department"
                  required
                />
              </div>
              <div class="mb-4 pl-2 grow w-full">
                <label
                  class="block text-gray-700 text-sm font-bold mb-2"
                  for="phone"
                >
                  เบอร์โทรศัพท์
                </label>
                <input
                  class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
                  id="phone"
                  type="text"
                  placeholder="Phone"
                  name="phone"
                  required
                />
              </div>
            </div>
            <div class="mb-4 flex items-baseline">
              <label
                for="category"
                class="block text-gray-700 text-sm font-bold mb-2 mr-3"
              >
                แจ้งปัญหา
              </label>
              <select
                name="category"
                id="category"
                class="py-1 px-3 shadow border rounded text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
              >
                <option value="IT">อุปกรณ์ IT</option>
              </select>
            </div>
            <div class="mb-4">
              <label
                for="description"
                class="block text-gray-700 text-sm font-bold mb-2 mr-3"
              >
                รายละเอียด
              </label>
              <textarea
                id="description"
                name="description"
                rows="4"
                class="w-full p-4 shadow border rounded text-gray-700 leading-tight focus:outline-none focus:shadow-outline"
              ></textarea>
            </div>
            <div class="flex justify-end">
              <button
                class="text-blue-500 hover:underline font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                type="reset"
              >
                รีเซ็ต
              </button>
              <button
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                type="submit"
              >
                ยืนยัน
              </button>
            </div>
            <!-- <div class="mb-6"> -->
            <!-- <label
                class="block text-gray-700 text-sm font-bold mb-2"
                for="password"
              >
                Password
              </label>
              <input
                class="shadow appearance-none border border-red-500 rounded w-full py-2 px-3 text-gray-700 mb-3 leading-tight focus:outline-none focus:shadow-outline"
                id="password"
                type="password"
                placeholder="******************"
              /> -->
            <!-- <p class="text-red-500 text-xs italic">
                Please choose a password.
              </p> -->
            <!-- </div> -->
            <!-- <div class="flex items-center justify-between">
              <button
                class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline"
                type="button"
              >
                Sign In
              </button>
            </div> -->
          </form>
        </div>
      </div>
    </main>
  </body>
</html>
